import {useEffect,useState} from "react";import {Link,useLocation,useSearchParams} from "react-router-dom";import api,{unwrap} from "../api";import {useAuth} from "../auth/AuthContext";
const categories=['JAVA','SPRING','DATABASE','REACT','NETWORK','DEPLOYMENT','GIT','ETC'];
export default function BoardListPage({boardType}){const{user}=useAuth();const loc=useLocation();const[params,setParams]=useSearchParams();const[data,setData]=useState({content:[],totalElements:0,totalPages:0,page:0});const[loading,setLoading]=useState(true);const[error,setError]=useState('');const keyword=params.get('keyword')??'';const category=params.get('category')??'';const mine=params.get('mine')==='true';useEffect(()=>{setLoading(true);setError('');api.get('/posts',{params:{boardType,keyword:keyword||undefined,category:boardType==='ERROR_WIKI'&&category?category:undefined,mine,page:params.get('page')??0,size:10}}).then(r=>setData(unwrap(r))).catch(e=>setError(e.userMessage??'게시글 목록을 불러오지 못했습니다.')).finally(()=>setLoading(false))},[boardType,keyword,category,mine,params]);function search(e){e.preventDefault();const f=new FormData(e.currentTarget);setParams({...(f.get('keyword')?{keyword:f.get('keyword')}:{ }),...(f.get('category')?{category:f.get('category')}:{ }),...(mine?{mine:'true'}:{})})}const title=boardType==='ERROR_WIKI'?'에러위키':'자유게시판';return <section className="mx-auto max-w-6xl px-4 py-10 sm:px-6"><div className="mb-7 flex items-end justify-between"><div><p className="font-bold text-[#ff512f]">ERROR WIKY</p><h1 className="mt-1 text-3xl font-black">{title}</h1><p className="mt-2 text-[#667085]">{boardType==='ERROR_WIKI'?'오류와 해결 과정을 검색하고 공유합니다.':'개발 이야기를 자유롭게 나눕니다.'}</p></div>{user&&<Link to={`/posts/new?boardType=${boardType}`} className="btn-primary">+ 게시글 작성</Link>}</div><div className="card overflow-hidden"><form onSubmit={search} className="flex flex-col gap-3 border-b border-[#e4e7ec] bg-[#fcfcfd] p-5 md:flex-row">{boardType==='ERROR_WIKI'&&<select name="category" defaultValue={category} className="input md:max-w-52"><option value="">전체 카테고리</option>{categories.map(c=><option key={c}>{c}</option>)}</select>}<input name="keyword" defaultValue={keyword} className="input flex-1" placeholder="제목 또는 내용 검색"/><button className="btn-primary">검색</button>{user&&<button type="button" className={`btn-secondary ${mine?'!border-[#ff512f] !text-[#ff512f]':''}`} onClick={()=>setParams(mine?{}:{mine:'true'})}>내 글</button>}</form><div className="flex items-center justify-between border-b border-[#e4e7ec] px-5 py-4 text-sm text-[#667085]"><span>총 <b className="text-[#101828]">{data.totalElements}</b>건</span><span>{mine?'내가 작성한 목록':'전체 공개 목록'}</span></div>{loading?<State text="불러오는 중..."/>:error?<State text={error} error/>:data.content.length===0?<State text="등록된 게시글이 없습니다."/>:<ul className="divide-y divide-[#e4e7ec]">{data.content.map(p=><li key={p.postId}><Link to={`/posts/${p.postId}`} state={{from:loc.pathname+loc.search}} className="grid gap-2 px-5 py-5 hover:bg-[#fff8f6] md:grid-cols-[1fr_120px_100px_160px] md:items-center"><div><b>{p.title}</b><div className="mt-1 text-xs text-[#98a2b3] md:hidden">{p.authorName}</div></div><span className="text-sm text-[#ff512f]">{p.category??'자유'}</span><span className="text-sm text-[#475467]">{p.authorName}</span><span className="text-xs text-[#667085]">조회 {p.viewCount} · 좋아요 {p.likeCount} · 댓글 {p.commentCount}</span></Link></li>)}</ul>}<div className="flex justify-center gap-2 border-t border-[#e4e7ec] p-5">{Array.from({length:data.totalPages},(_,i)=>i).slice(Math.max(0,data.page-2),data.page+3).map(i=><button key={i} className={i===data.page?'btn-primary !px-3 !py-2':'btn-secondary !px-3 !py-2'} onClick={()=>{const n=new URLSearchParams(params);n.set('page',i);setParams(n)}}>{i+1}</button>)}</div></div></section>}
function State({text,error}){return <div className={`p-16 text-center ${error?'text-red-600':'text-[#667085]'}`}>{text}</div>}
